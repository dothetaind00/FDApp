<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tai06.dothetai.fdapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity android:name=".Activity.ViewMoreActivity"></activity>
        <activity android:name=".LoginSignup.ChangepswActivity" />
        <activity android:name=".LoginSignup.SignupActivity" />
        <activity android:name=".LoginSignup.LoginActivity" />
        <activity android:name=".Activity.HoaDonActivity" />
        <activity android:name=".Activity.InfoDrinkActivity" />
        <activity android:name=".Activity.InfoFoodActivity" />
        <activity android:name=".Activity.SearchViewActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>